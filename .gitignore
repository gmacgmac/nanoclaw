# Dependencies
node_modules/
.npm-cache/
# Build output
dist/

# Local data & auth
store/
data/
logs/

# Groups - runtime group folders are local-only
groups/*

# Secrets
*.keys.json
.env

# Legacy SQLite database (migrated to PostgreSQL)
*.db
*.sqlite

# tool-allowlist.json is intentionally TRACKED (security ceiling) — do not ignore

# mcp-catalog.json is intentionally UNTRACKED (regenerated at API startup)
mcp-catalog.json

# Prompt behaviour snippets (operational IP, not public — remove after de-fork)
# docs/prompt-behaviours/

# Temp files
.tmp-*

# OS
.DS_Store

# IDE
.idea/
.vscode/

# Skills system (local per-installation state)
.nanoclaw/

agents-sdk-docs