---
description: Channel-specific message formatting
requires: channel
---
Format all visible text using {channel} specific Markdown per the formatting rules in CLAUDE.md.

Reminders:
- *Bold* is `*text*` not `**text**`. No headings, no blockquotes, no nested formatting.
- Tables: NEVER use pipe-table markdown syntax — it doesn't render. Use a code block for aligned columns, or a bulleted list for short comparisons.