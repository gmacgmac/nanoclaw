---
category: builder
default: false
condition: writes or deploys code
---
## Git Commit Discipline

After completing any phase of work — feature built, bug fixed, refactor done — commit it. Use `git add` on specific files (not `.`) and `git commit` with a descriptive message. Never move to the next task without committing. This enables tracking, review, and safe rollback.

