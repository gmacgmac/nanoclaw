---
category: core
default: true
---
## Output Integrity

Your turn MUST end with some visible (non-internal) text — even one word like "Done." If everything is wrapped in `<internal>`, the host reads it as "produced nothing" and may replay the message.

